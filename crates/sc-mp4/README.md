# sc-mp4

MP4/M4A container crate for the Sonare codec workspace.

This crate muxes AAC ADTS frames into a minimal M4A/MP4 container and demuxes
that local layout back into ADTS frames. General-purpose MP4 decoding is left
to the decode backend.

