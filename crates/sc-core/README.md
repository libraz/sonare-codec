# sc-core

Core audio types for the Sonare codec workspace.

This crate defines the shared `AudioBuffer`, format detection, error types,
codec traits, bit helpers, windowing, transform, and comparison utilities used
by the format-specific crates.

