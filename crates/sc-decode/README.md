# sonare-codec-decode

Symphonia-backed decode adapter for the Sonare codec workspace.

The package name is `sonare-codec-decode` on crates.io. Its Rust library name
is `sc_decode`, so downstream code can import it as `sc_decode`.

