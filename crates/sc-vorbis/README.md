# sc-vorbis

Vorbis codec crate for the Sonare codec workspace.

This crate provides Ogg Vorbis decode through the shared Symphonia decode
adapter and Ogg Vorbis encode through `vorbis_rs`/libvorbis in quality-based
VBR mode.
